<%= form_for(@level) do |f| %>
  <% if @level.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @level.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group">
    <%= f.label :Badge %><br>
   <%= collection_select(:level, :badge_id,Badge.all, :id, :title ,:prompt => false,:class => 'form-control') %>
   <br>
   <br>

</div>


    <div class="field">
    <%= f.label :Order %><br>
   After Level: <%= collection_select(:level, :order,Level.all, :order,:order ,:prompt => false,:class => 'form-control') %>
   <br>
   <br>
    <%#=Level.maximum("order")%>

    </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
