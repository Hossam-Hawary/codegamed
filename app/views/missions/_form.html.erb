<%= form_for(@mission) do |f| %>
  <% if @mission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mission.errors.count, "error") %> prohibited this mission from being saved:</h2>

      <ul>
      <% @mission.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group">
    <%= f.label :Level %><br>
   <%= collection_select(:mission, :level_id,Level.order("levels.order").all, :id, :order ,:prompt => false,:class => 'form-control',:selected =>1) %>
   <br>
   <br>

</div>


    <div class="field">
    <%= f.label :Order %><br>
    <%= f.number_field :order%>
  <%#= collection_select(:mission, :order,Level.find(5).missions.order("missions.order"), :order,:order ,:prompt => false,:class => 'form-control') %>
   <br>
   <br>
    </div>
       <div class="field">
        <%= f.label :VideoURL  %><br>
        <%= f.text_field :video_url%>
       <br>
       <br>
      </div>
      <div class="field">
              <%= f.label :Score %><br>
              <%= f.number_field :score%>
              <br>
              <br>
      </div>
      <div class="field">
         <%= f.label :Problem %><br>
         <%= f.text_area :problem, autofocus: true ,:class => 'ckeditor form-control' %>
          <br>
          <br>
      </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
